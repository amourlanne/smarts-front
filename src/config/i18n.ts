import frMessages from "@/translations/messages.fr";
import enMessages from "@/translations/messages.en";

export default {
  locale: "en",
  fallbackLocale: "en",
  messages: {
    fr: frMessages,
    en: enMessages
  }
};
